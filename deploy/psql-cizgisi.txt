# DOĞRUDAN psql ÇAĞRISI ÇİZGİSİ (Y3).
# Ölçüm psql'i deploy/scripts/vt-sorgu.sh üzerinden geçer.
# Buradaki satırlar işlevsel betikler: yedek, göç, rig, kapı.
# LİSTE YALNIZCA KÜÇÜLÜR. Yeni bir doğrudan çağrı eklenemez.
# Biçim: dosya:adet
#
# ═══ 2026-09-14 EKLEMESİ — GEREKÇE ═══
#
# `scripts/enderun-kurtarma.sh` (PAKET C, felaket anı geri yükleme)
# doğrudan psql kullanıyor ve BU BİLEREK YAPILDI:
#
#   Kurtarma betiği SON SAVUNMA HATTIDIR ve depo yolundan BAĞIMSIZ
#   koşabilmelidir. `vt-sorgu.sh`e bağlamak, felaket anında
#   `/var/www/enderun-ai/deploy/...` erişilebilir olmasına bağımlılık
#   yaratırdı — kurtarmanın gerektiği an tam da o dizinin eksik
#   olabileceği andır. (Aynı gerekçeyle `say.sh` de yedek betiğine
#   bağlanmadı.)
#
# Çağrıların hepsi hedefi AÇIKÇA veriyor (`-d "$HEDEF"`, `-d postgres`);
# `vt-sorgu.sh`in koruduğu "adsız çağrı sessizce postgres'e bağlanır"
# tuzağı burada yok. Ayrıca betiğin kendi muhafızları var: bakım
# veritabanı reddi, canlı hedef için ikinci onay, damga doğrulaması.
deploy/scripts/acik-veritabani-kapisi.sh:2
deploy/scripts/departman-dagilimi.sh:1
deploy/scripts/duzen-testi.sh:2
deploy/scripts/goc-provasi.sh:10
deploy/scripts/goc-uygula.sh:1
deploy/scripts/safe-deploy.sh:1
deploy/scripts/seed-sondasi.sh:2
deploy/scripts/sema-karsilastir.sh:1
deploy/scripts/sema-sapma-kapisi.sh:1
scripts/enderun-backup.sh:2
scripts/enderun-geri-yukleme-tatbikati.sh:1
scripts/enderun-yedek-uzak.py:1
scripts/enderun-kurtarma.sh:5
